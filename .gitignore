# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.cursor/settings.json
# Local: enable Cursor agent auto-continue (see .cursor/WORKER_LOOP.md)
.cursor/worker-loop.enabled
.cursor/worker-loop.disabled
.cursor/worker-loop.after-response
.cursor/hooks/auto-continue.log
.cursor/hooks/hooks-smoke.log
.cursor/hooks/after-agent-response.log
.cursor/hooks/after-agent-response-state.json
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.angular/
__pycache__/

# Local environment files (backend)
vehapiproxi/.env
vehapiproxi/.env.local

# Live Motor session cookies (multi-session seed)
vehapiproxi/sessions.json

# Unrelated local tooling (SIP/VoIP experiments, IDE configs)
pjsua.conf
dump_pinger.py
.gemini/

# Bulk ingest raw dumps (worker-ingest-vehicles-full.js; cwd is usually vehapiproxi)
data/raw/
vehapiproxi/data/raw/
vehapiproxi/.env.vercel.prod.check
vehapiproxi/.env*.local

# Local dev / test artifacts (not for the repo)
backups/
test-results/

# Bulk Motor ingest CLI L0 snapshots (see vehapiproxi npm run worker:ingest-full)
data/raw/
data/raw/
